# checkers-minimax
A checkers minimax implementation which uses alpha-beta pruning to reduce the state space of the problem.


## C++ Compile:
``` bash
g++ -O3 -std=c++14 -shared -fPIC -o engine.so engine.cpp
```
## Then run Python
``` bash
python3 gui.py || main.py
```
